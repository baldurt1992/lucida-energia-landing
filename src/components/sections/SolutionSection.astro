<section id="solucion" class="solution" aria-labelledby="solucion-titulo">
  <div class="shell">
    <header class="intro">
      <h2 id="solucion-titulo" data-reveal="statement">
        Una revisión energética, explicada en lenguaje humano.
      </h2>
      <p>
        Traducimos luz y gas a una lectura que se puede seguir. Buscamos
        posibles mejoras; no adelantamos un resultado.
      </p>
    </header>

    <figure class="solution-image" data-reveal="mask">
      <img
        src="https://images.pexels.com/photos/6963909/pexels-photo-6963909.jpeg?auto=compress&cs=tinysrgb&w=1200"
        srcset="https://images.pexels.com/photos/6963909/pexels-photo-6963909.jpeg?auto=compress&cs=tinysrgb&w=640 640w, https://images.pexels.com/photos/6963909/pexels-photo-6963909.jpeg?auto=compress&cs=tinysrgb&w=1200 1200w, https://images.pexels.com/photos/6963909/pexels-photo-6963909.jpeg?auto=compress&cs=tinysrgb&w=1600 1600w"
        sizes="(min-width: 1100px) 42vw, 100vw"
        width="1200"
        height="800"
        loading="lazy"
        decoding="async"
        alt="Pareja revisando documentos y gastos del hogar en una mesa."
      />
      <figcaption>
        Primero entendemos el contexto. Después, los números.
      </figcaption>
    </figure>

    <dl class="translation" data-reveal="translation">
      <div class="pair">
        <dt>Diagnóstico</dt>
        <dd>Qué dice la factura, en el orden en que se entiende.</dd>
      </div>
      <div class="pair">
        <dt>Luz y gas</dt>
        <dd>
          Cada suministro con su contrato, sin mezclarlos en un solo discurso.
        </dd>
      </div>
      <div class="pair">
        <dt>Condiciones</dt>
        <dd>Lo que está escrito, no un reclamo comercial.</dd>
      </div>
      <div class="pair">
        <dt>Recomendación</dt>
        <dd>
          Una propuesta adaptada a tu caso. Si no vemos una mejora clara,
          también te lo decimos.
        </dd>
      </div>
      <div class="pair">
        <dt>Acompañamiento</dt>
        <dd>Seguimos el proceso contigo, al ritmo en que puedas decidir.</dd>
      </div>
    </dl>
  </div>
</section>

<style>
  .solution {
    box-sizing: border-box;
    padding-block: var(--space-section, var(--section-space));
    background: var(--color-mineral);
    color: var(--color-ink);
    font-family: var(--font-body);
    overflow-x: clip;
  }

  .solution *,
  .solution *::before,
  .solution *::after {
    box-sizing: border-box;
  }

  .shell {
    width: min(100%, var(--container, var(--page-max)));
    margin-inline: auto;
    padding-inline: var(--gutter, clamp(1rem, 4vw, 2rem));
  }

  .intro {
    display: grid;
    gap: 1.25rem;
    margin-block-end: clamp(2rem, 5vw, 3.5rem);
  }

  .intro h2 {
    margin: 0;
    max-width: 16ch;
    font-family: var(--font-display);
    font-size: clamp(2.05rem, 1rem + 3.6vw, 5rem);
    font-weight: 650;
    line-height: 0.98;
    letter-spacing: -0.03em;
    text-wrap: balance;
    overflow-wrap: break-word;
  }

  .intro p {
    margin: 0;
    max-width: 42ch;
    font-size: 1.0625rem;
    line-height: 1.55;
    color: var(--color-muted, var(--color-ink-soft));
  }

  .translation {
    margin: 0;
  }

  .solution-image {
    position: relative;
    margin: 0 0 clamp(2.5rem, 6vw, 5rem);
    overflow: hidden;
    background: var(--color-petroleum);
  }

  .solution-image img {
    width: 100%;
    height: clamp(18rem, 52vw, 38rem);
    object-fit: cover;
    object-position: center 42%;
    filter: saturate(0.8) contrast(1.04);
  }

  .solution-image figcaption {
    position: absolute;
    right: 0;
    bottom: 0;
    max-width: 23rem;
    padding: 1rem 1.25rem;
    background: var(--color-petroleum);
    color: var(--color-mineral);
    font-size: var(--text-sm);
    line-height: 1.4;
  }

  .pair {
    display: grid;
    grid-template-columns: minmax(0, 1fr);
    gap: 0.35rem;
    padding-block: clamp(1rem, 2vw, 1.35rem);
    border-top: 1px solid color-mix(in srgb, var(--color-ink) 16%, transparent);
  }

  .pair:last-child {
    border-bottom: 1px solid
      color-mix(in srgb, var(--color-ink) 16%, transparent);
  }

  .pair dt {
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(1.45rem, 1rem + 1.2vw, 2.15rem);
    font-weight: 600;
    letter-spacing: -0.03em;
    line-height: 1.1;
  }

  .pair dd {
    margin: 0;
    max-width: 48ch;
    font-size: 1.0625rem;
    line-height: 1.5;
  }

  @media (min-width: 720px) {
    .pair {
      grid-template-columns: minmax(9.5rem, 16rem) minmax(0, 1fr);
      column-gap: clamp(1.5rem, 4vw, 4rem);
      align-items: baseline;
    }

    .pair:nth-child(even) dd {
      max-width: 42ch;
    }
  }

  @media (min-width: 1100px) {
    .intro {
      grid-template-columns: minmax(0, 1.15fr) minmax(16rem, 0.7fr);
      align-items: end;
      column-gap: clamp(2rem, 5vw, 5rem);
    }

    .intro p {
      justify-self: end;
    }

    .solution-image {
      width: calc(100% - clamp(4rem, 12vw, 11rem));
      margin-left: auto;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .solution {
      scroll-behavior: auto;
    }
  }
</style>
