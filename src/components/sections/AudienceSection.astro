<section id="para-quien" class="audience" aria-labelledby="audiencia-titulo">
  <div class="shell">
    <header class="intro">
      <h2 id="audiencia-titulo" data-reveal="audience">
        Hogares, autónomos y pymes
      </h2>
      <p>La misma revisión, leída desde tres situaciones distintas.</p>
    </header>

    <div class="audience-grid">
      <figure class="audience-image" data-reveal="mask">
        <img
          src="https://images.pexels.com/photos/37076986/pexels-photo-37076986.jpeg?auto=compress&cs=tinysrgb&w=1200"
          srcset="https://images.pexels.com/photos/37076986/pexels-photo-37076986.jpeg?auto=compress&cs=tinysrgb&w=640 640w, https://images.pexels.com/photos/37076986/pexels-photo-37076986.jpeg?auto=compress&cs=tinysrgb&w=1200 1200w"
          sizes="(min-width: 800px) 42vw, 100vw"
          width="1200"
          height="900"
          loading="lazy"
          decoding="async"
          alt="Artesano trabajando en su taller de Madrid."
        />
        <figcaption>
          Un contrato energético también forma parte del trabajo diario.
        </figcaption>
      </figure>

      <div class="profiles">
        <article class="profile profile-home">
          <h3>Hogares</h3>
          <p>
            La factura de casa junta términos que no se explican entre sí. Hace
            falta una lectura antes de tocar el contrato.
          </p>
        </article>

        <article class="profile profile-solo">
          <h3>Autónomos</h3>
          <p>
            El suministro del trabajo se decide sin un equipo que traduzca la
            potencia, el precio o los servicios añadidos.
          </p>
        </article>

        <article class="profile profile-company">
          <h3>Pymes</h3>
          <p>
            Puede haber más de un punto de suministro y contratos que no
            comparten el mismo criterio.
          </p>
        </article>
      </div>
    </div>
  </div>
</section>

<style>
  .audience {
    box-sizing: border-box;
    padding-block: var(--space-section, var(--section-space));
    background: var(--color-petrol, var(--color-petroleum));
    color: var(--color-mineral);
    font-family: var(--font-body);
    overflow-x: clip;
  }

  .audience *,
  .audience *::before,
  .audience *::after {
    box-sizing: border-box;
  }

  .shell {
    width: min(100%, var(--container, var(--page-max)));
    margin-inline: auto;
    padding-inline: var(--gutter, clamp(1rem, 4vw, 2rem));
  }

  .intro {
    display: grid;
    gap: 0.85rem;
    margin-block-end: clamp(1.5rem, 4vw, 2.75rem);
    max-width: 40rem;
  }

  .intro h2 {
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(2.05rem, 1rem + 3.4vw, 4.75rem);
    font-weight: 650;
    line-height: 0.98;
    letter-spacing: -0.03em;
    text-wrap: balance;
  }

  .intro p {
    margin: 0;
    max-width: 36ch;
    font-size: 1.0625rem;
    line-height: 1.5;
    color: var(--color-mineral-soft);
  }

  .profiles {
    display: grid;
  }

  .audience-grid {
    display: grid;
    gap: clamp(2rem, 6vw, 5rem);
  }

  .audience-image {
    position: relative;
    margin: 0;
    min-height: 24rem;
    overflow: hidden;
  }

  .audience-image img {
    width: 100%;
    height: 100%;
    min-height: 24rem;
    object-fit: cover;
    object-position: center;
    filter: saturate(0.72) contrast(1.08);
  }

  .audience-image::after {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgb(7 24 22 / 78%), transparent 50%);
    pointer-events: none;
  }

  .audience-image figcaption {
    position: absolute;
    z-index: 1;
    right: 1.25rem;
    bottom: 1.25rem;
    left: 1.25rem;
    max-width: 30ch;
    color: var(--color-mineral);
    font-size: var(--text-sm);
    line-height: 1.45;
  }

  .profile {
    display: grid;
    grid-template-columns: minmax(0, 1fr);
    gap: 0.45rem;
    padding-block: clamp(1.15rem, 2.5vw, 1.85rem);
    border-top: 1px solid
      var(
        --color-line-deep,
        color-mix(in srgb, var(--color-mineral) 28%, transparent)
      );
  }

  .profile:last-child {
    border-bottom: 1px solid
      var(
        --color-line-deep,
        color-mix(in srgb, var(--color-mineral) 28%, transparent)
      );
  }

  .profile h3 {
    margin: 0;
    font-family: var(--font-display);
    font-weight: 640;
    letter-spacing: -0.035em;
    line-height: 0.95;
    text-wrap: balance;
  }

  .profile-home h3 {
    font-size: clamp(2.6rem, 1.4rem + 5vw, 5.5rem);
  }

  .profile-solo h3 {
    font-size: clamp(2rem, 1.1rem + 3vw, 3.6rem);
  }

  .profile-company h3 {
    font-size: clamp(2.35rem, 1.2rem + 4vw, 4.75rem);
  }

  .profile p {
    margin: 0;
    max-width: 38ch;
    font-size: 1.0625rem;
    line-height: 1.5;
    color: var(--color-mineral);
  }

  @media (min-width: 800px) {
    .audience-grid {
      grid-template-columns: minmax(18rem, 0.72fr) minmax(0, 1.28fr);
      align-items: stretch;
    }

    .audience-image {
      min-height: 42rem;
    }

    .profile {
      grid-template-columns: minmax(0, 1fr);
      gap: 0.85rem;
      align-content: start;
      padding-block: clamp(1.5rem, 3vw, 2.25rem);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .audience {
      scroll-behavior: auto;
    }
  }
</style>
