<section
  id="problema"
  class="problem"
  aria-labelledby="problema-titulo"
>
  <div class="shell">
    <div class="layout" data-reveal="lift">
      <h2 id="problema-titulo" class="title">
        Pagas energía,<br />
        no <em class="mark">confusión.</em>
      </h2>

      <div class="copy">
        <ul class="pains">
          <li>Tarifas difíciles de comparar.</li>
          <li>Potencia que puede no encajar con el uso.</li>
          <li>Condiciones y servicios poco claros.</li>
          <li>Poco tiempo para revisar el mercado.</li>
        </ul>
        <p class="reading">
          La factura cobra la energía y, a la vez, una lectura que casi nunca
          viene explicada.
        </p>
      </div>

      <div class="scraps" aria-hidden="true">
        <p class="scrap">Tarifa con otro nombre · condición aparte</p>
        <p class="scrap scrap-shift">Potencia contratada · término fijo</p>
        <p class="scrap scrap-shift-more">Servicio añadido · sin contexto</p>
      </div>
    </div>
  </div>
</section>

<style>
  .problem {
    box-sizing: border-box;
    padding-block: var(--space-section, var(--section-space));
    background: var(--color-mineral);
    color: var(--color-ink);
    font-family: var(--font-body);
    overflow-x: clip;
  }

  .problem *,
  .problem *::before,
  .problem *::after {
    box-sizing: border-box;
  }

  .shell {
    width: min(100%, var(--container, var(--page-max)));
    margin-inline: auto;
    padding-inline: var(--gutter, clamp(1rem, 4vw, 2rem));
  }

  .layout {
    display: grid;
    gap: clamp(1.75rem, 4vw, 3.5rem);
  }

  .title {
    margin: 0;
    max-width: 12ch;
    font-family: var(--font-display);
    font-size: clamp(2.1rem, 1rem + 4.2vw, 5.5rem);
    font-weight: 650;
    line-height: 0.96;
    letter-spacing: -0.03em;
    text-wrap: balance;
    overflow-wrap: break-word;
  }

  .mark {
    font-style: normal;
    color: var(--color-ink);
    background-image: linear-gradient(
      var(--color-current, var(--color-electric)),
      var(--color-current, var(--color-electric))
    );
    background-repeat: no-repeat;
    background-size: 100% 0.14em;
    background-position: 0 92%;
  }

  .copy {
    display: grid;
    gap: clamp(1.25rem, 2.5vw, 1.75rem);
    align-content: end;
    min-width: 0;
  }

  .pains {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: clamp(0.85rem, 2vw, 1.35rem);
  }

  .pains li {
    max-width: 22ch;
    font-family: var(--font-display);
    font-size: clamp(1.35rem, 1rem + 1.4vw, 2rem);
    font-weight: 560;
    line-height: 1.15;
    letter-spacing: -0.025em;
    text-wrap: balance;
  }

  .pains li:first-child {
    font-size: clamp(1.7rem, 1.1rem + 2.2vw, 2.75rem);
  }

  .scraps {
    display: grid;
    align-content: end;
    min-width: 0;
  }

  .scrap {
    margin: 0;
    padding-block: 0.8rem;
    border-top: 1px solid color-mix(in srgb, var(--color-ink) 18%, transparent);
    font-family: var(--font-display);
    font-size: clamp(1.02rem, 0.9rem + 0.6vw, 1.35rem);
    font-weight: 500;
    line-height: 1.3;
    letter-spacing: -0.02em;
    color: var(--color-muted, var(--color-ink-soft));
  }

  .scrap-shift {
    padding-inline-start: 8%;
  }

  .scrap-shift-more {
    padding-inline-start: 16%;
  }

  .reading {
    margin: 0;
    max-width: 38ch;
    font-size: 1rem;
    line-height: 1.55;
    color: var(--color-muted, var(--color-ink-soft));
  }

  @media (min-width: 960px) {
    .layout {
      grid-template-columns: minmax(25rem, 1.05fr) minmax(22rem, 0.95fr);
      column-gap: clamp(2.5rem, 5vw, 5rem);
      row-gap: clamp(2.5rem, 5vw, 4rem);
      align-items: start;
    }

    .title {
      grid-column: 1;
      max-width: 13ch;
      font-size: clamp(3rem, 4.6vw, 4.5rem);
    }

    .copy {
      grid-column: 2;
      padding-top: 0.35rem;
    }

    .pains {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 0;
      border-top: 1px solid color-mix(in srgb, var(--color-ink) 18%, transparent);
    }

    .pains li,
    .pains li:first-child {
      max-width: none;
      min-height: 7rem;
      padding: 1rem 1.25rem 1rem 0;
      border-bottom: 1px solid color-mix(in srgb, var(--color-ink) 18%, transparent);
      font-size: clamp(1.2rem, 1rem + 0.55vw, 1.55rem);
      display: flex;
      align-items: flex-start;
    }

    .pains li:nth-child(odd) {
      border-right: 1px solid color-mix(in srgb, var(--color-ink) 18%, transparent);
    }

    .pains li:nth-child(even) {
      padding-left: 1.25rem;
    }

    .reading {
      max-width: 46ch;
    }

    .scraps {
      grid-column: 1 / -1;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: clamp(1.5rem, 4vw, 3.5rem);
    }

    .scrap,
    .scrap-shift,
    .scrap-shift-more {
      padding-inline-start: 0;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .problem {
      scroll-behavior: auto;
    }
  }

  @media (forced-colors: active) {
    .mark {
      background-image: none;
      text-decoration: underline;
    }
  }
</style>
